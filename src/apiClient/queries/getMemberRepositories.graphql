query memberRepositories($login: String!, $cursor: String) {
  user(login: $login) {
    repositoriesContributedTo(first: 10, after: $cursor) {
      nodes {
        description
        name
        url
      }
    }
  }
}
